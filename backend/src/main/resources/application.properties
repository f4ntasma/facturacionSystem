# Configuración del servidor
server.port=8081

# Base de datos
spring.datasource.url=${DB_URL}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# HikariCP
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.connection-test-query=SELECT 1

# JWT
security.jwt.secret=${JWT_SECRET}
security.jwt.expiration=86400000

# Logging
logging.level.com.facturacion=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG

# ========================================
# CONFIGURACION SUNAT
# ========================================

# Credenciales SUNAT
sunat.ruc=20000000001
sunat.usuario=MODDATOS
sunat.clave=MODDATOS

# URLs de servicios SUNAT - BETA
sunat.url-factura=https://e-beta.sunat.gob.pe/ol-ti-itcpfegem-beta/billService
sunat.url-guia=https://e-beta.sunat.gob.pe/ol-ti-itemision-guia-gem-beta/billService
sunat.url-retenciones=https://e-beta.sunat.gob.pe/ol-ti-itemision-otroscpe-gem-beta/billService

# URLs de servicios SUNAT - PRODUCCION
#sunat.url-factura=https://e-beta.sunat.gob.pe/ol-ti-itcpfegem-beta/billService
#sunat.url-guia=https://e-beta.sunat.gob.pe/ol-ti-itemision-guia-gem-beta/billService
#sunat.url-retenciones=https://e-beta.sunat.gob.pe/ol-ti-itemision-otroscpe-gem-beta/billService

# Certificado
sunat.certificado-base64=${CERT_PFX:}
sunat.certificado-password=${CERT_PFX_PASSWORD:}

# Ambiente (false = BETA, true = PRODUCCION)
sunat.produccion=false

# Datos de la empresa emisora
sunat.razon-social=MI EMPRESA S.A.C.
sunat.nombre-comercial=MI EMPRESA
sunat.direccion=AV. EJEMPLO 123
sunat.ubigeo=150101
sunat.departamento=LIMA
sunat.provincia=LIMA
sunat.distrito=LIMA
sunat.urbanizacion=
sunat.codigo-pais=PE

# Services Videos
apiperu.token=${APIPERU_TOKEN}

# Mercado Pago
mercadopago.access-token=${MP_ACCESS_TOKEN}
mercadopago.public-key=${MP_PUBLIC_KEY}

# Frontend
app.frontend-url=${FRONTEND_URL}